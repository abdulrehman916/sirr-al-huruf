{
  "name": "UserAccessProfile",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Base44 User ID (foreign key)"
    },
    "full_name": {
      "type": "string",
      "description": "User full name from platform account"
    },
    "mobile": {
      "type": "string",
      "description": "User mobile number with country code"
    },
    "email": {
      "type": "string",
      "description": "User email address"
    },
    "mobile_verified": {
      "type": "boolean",
      "default": false,
      "description": "Is mobile number verified via OTP"
    },
    "email_verified": {
      "type": "boolean",
      "default": false,
      "description": "Is email verified via OTP"
    },
    "role": {
      "type": "string",
      "enum": [
        "user",
        "admin"
      ],
      "default": "user",
      "description": "User role from platform account"
    },
    "registration_date": {
      "type": "string",
      "description": "ISO 8601 timestamp of registration"
    },
    "last_login": {
      "type": "string",
      "description": "ISO 8601 timestamp of last login"
    },
    "account_status": {
      "type": "string",
      "enum": [
        "ACTIVE",
        "SUSPENDED",
        "DEACTIVATED",
        "BLOCKED",
        "ARCHIVED"
      ],
      "default": "ACTIVE",
      "description": "Current account status. BLOCKED = cannot login/OTP/access. ARCHIVED = hidden, cannot login, records preserved."
    },
    "blocked_at": {
      "type": "string",
      "description": "ISO 8601 timestamp when account was blocked/archived"
    },
    "blocked_by": {
      "type": "string",
      "description": "Admin user ID who blocked/archived this account"
    },
    "block_reason": {
      "type": "string",
      "description": "Reason for blocking or archiving the account"
    },
    "archived_at": {
      "type": "string",
      "description": "ISO 8601 timestamp when account was archived"
    },
    "archived_by": {
      "type": "string",
      "description": "Admin user ID who archived this account"
    },
    "archive_reason": {
      "type": "string",
      "description": "Reason for archiving"
    },
    "subscription_plan": {
      "type": "string",
      "enum": [
        "1_MONTH",
        "3_MONTHS",
        "6_MONTHS",
        "12_MONTHS",
        "LIFETIME",
        "NONE"
      ],
      "default": "NONE",
      "description": "Current active subscription plan"
    },
    "subscription_expiry": {
      "type": "string",
      "description": "ISO 8601 timestamp of subscription expiry"
    },
    "lifetime_access": {
      "type": "boolean",
      "default": false,
      "description": "Has lifetime access to all pages"
    },
    "total_permissions": {
      "type": "integer",
      "default": 0,
      "description": "Count of all permissions ever granted"
    },
    "active_permissions": {
      "type": "integer",
      "default": 0,
      "description": "Count of currently active permissions"
    },
    "device_type": {
      "type": "string",
      "enum": [
        "mobile",
        "desktop",
        "tablet",
        "unknown"
      ],
      "default": "unknown",
      "description": "Device type from last session"
    },
    "country": {
      "type": "string",
      "description": "Country code from IP (e.g. AE, IN, US)"
    }
  },
  "required": [
    "user_id",
    "email",
    "registration_date",
    "account_status"
  ],
  "rls": {
    "create": true,
    "read": {
      "$or": [
        {
          "user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}